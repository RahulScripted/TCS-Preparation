// Compute the sum of first n natural numbers



#include <bits/stdc++.h>
using namespace std;


int main(){
    int num;
    cout<<"Enter number : ";
    cin>>num;

    cout<<"Sum of first "<<num<<" natural numbers will be : "<<(num * (num + 1) / 2);
}